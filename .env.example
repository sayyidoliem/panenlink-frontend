# Local LLM service URL (FastAPI on local_llm_service.py)
# Default: http://localhost:8000
# Change this if running the service on a different host/port, or using HF Inference Endpoints
LLM_SERVICE_URL=http://localhost:8000
